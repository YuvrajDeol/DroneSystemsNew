.venv/
__pycache__/
*.pyc
.DS_Store
exports/
exported_txt_1/
evaluation/
clutter_diagnostics.png

# Stale embedded copy of an earlier project iteration (was a broken
# gitlink — kept locally only, never tracked).
sentinel-missile-sim/

# Generated simulation outputs (large, reproducible via the pipeline —
# `python main.py --seed 42` then `python integrate_simulations.py --seed 42`).
integrated_output/

# Generated trajectory logs (reproducible via `python main.py --seed 42`).
logs/

# Generated training data, but keep the metadata index.
sentinel_training_data/*
!sentinel_training_data/metadata.csv
